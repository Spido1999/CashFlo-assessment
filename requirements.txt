# Core
pydantic>=2.7.0

# PDF parsing (optional — only needed if parsing .pdf policy files)
pdfplumber>=0.11.0

# LLM providers — install whichever you use
# FREE: Groq (recommended) — get key at https://console.groq.com
groq>=0.9.0
# OpenAI
openai>=1.30.0
# Anthropic Claude
anthropic>=0.28.0

# Environment variable loading
python-dotenv>=1.0.0

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0
